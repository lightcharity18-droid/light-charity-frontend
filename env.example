# Light Charity Frontend - Environment Variables
# Copy this file to .env.local and fill in your actual values

# Backend API URL (required) - MUST be prefixed with NEXT_PUBLIC_ for client-side access
NEXT_PUBLIC_BACKEND_URL=https://your-backend-url.onrender.com

# Optional: WebSocket URL (usually same as BACKEND_URL)
# NEXT_PUBLIC_WS_URL=https://your-backend-url.onrender.com

# Development environment example:
# NEXT_PUBLIC_BACKEND_URL=http://localhost:5000

# Production environment example:
# NEXT_PUBLIC_BACKEND_URL=https://light-charity-backend.onrender.com

# Notes:
# - NEXT_PUBLIC_BACKEND_URL should point to your deployed backend service
# - For local development, use http://localhost:5000 (or your backend port)
# - For production, use your Render backend URL
# - Do not include trailing slashes in URLs
# - NEXT_PUBLIC_ prefix is REQUIRED for client-side access in Next.js
